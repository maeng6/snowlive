import 'package:flutter/material.dart';

class TreasureHuntListView extends StatelessWidget {
  const TreasureHuntListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
