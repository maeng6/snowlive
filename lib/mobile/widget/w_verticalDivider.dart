import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

Widget buildVerticalDivider_ranking_indi_Screen() {
  return Container(
    height: 38, // 구분선 높이 설정
    width: 1, // 구분선 너비 설정
    color: Color(0xFF444444).withOpacity(0.1), // 구분선 색상 설정
  );
}